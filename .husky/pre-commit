#!/bin/sh
# Husky disabled - uncomment and restore logic below if you want hooks back
# . "$(dirname "$0")/common.sh"
# REPO_ROOT="$(cd "$(dirname "$0")/.." && pwd)"
# cd "$REPO_ROOT"
# ... branch check, lint-staged ...
exit 0
